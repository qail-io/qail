# Changelog

For the full project changelog, see the repository file:

- [`CHANGELOG.md`](https://github.com/qail-io/qail/blob/main/CHANGELOG.md)

## Current Highlights (v0.27.9)

- Build scanner now correctly handles inline `//` comments inside `const` column arrays without dropping trailing columns.
- Explicit apply parsing now supports mixed enum/alter/index command blocks in one migration file.
- Workspace crates and docs references were bumped to `0.27.9`.
