{
    "name": "qail-mcp-worker",
    "version": "2.0.0-rc.1",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "./build.sh",
        "dev": "npm run build && wrangler dev",
        "deploy": "npm run build && wrangler deploy",
        "tail": "wrangler tail"
    },
    "devDependencies": {
        "wrangler": "^4.114.0"
    },
    "overrides": {
        "undici": "7.29.0"
    }
}
