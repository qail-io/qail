# PostgreSQL HBA for Kerberos-only auth.
# TYPE  DATABASE        USER            ADDRESS         METHOD
local   all             all                             trust
host    all             all             0.0.0.0/0       gss include_realm=0 krb_realm=TEST.QAIL.IO
host    all             all             ::/0            gss include_realm=0 krb_realm=TEST.QAIL.IO
